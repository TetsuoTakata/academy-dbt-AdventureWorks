version: 2

models:
  - name: fct_vendas
    description: fato vendas
    columns:
      - name: id_vendas
        description: chave primária da fato vendas
        tests:
          - unique
          - not_null
              