version: 2

models:
  - name: fct_vendas
    description: fato vendas
    columns:
      - name: sk_pedido
        description: chave surrogate da fato vendas
        tests:
          - unique
          - not_null
              